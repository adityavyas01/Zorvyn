# Required in production. Must be HTTPS in production builds.
VITE_API_URL=https://your-backend-domain.onrender.com

# Legacy compatibility fallback (optional during transition).
# Prefer VITE_API_URL.
# VITE_API_BASE_URL=https://your-backend-domain.onrender.com
